import React from 'react'
import './Nav.css'
import RandomUser from '../randomUser/RandomUser'

const Nav = () => {
    return (
        <nav>
            <RandomUser />
        </nav>
    )
}

export default Nav

const styles = {
    
}